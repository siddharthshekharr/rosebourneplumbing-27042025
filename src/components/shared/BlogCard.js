import Link from 'next/link'
import Image from 'next/image'

export default function BlogCard({ post }) {
    return (
        <div className="bg-white rounded-xl shadow-lg overflow-hidden">
            {/* Blog card content will go here */}
        </div>
    )
} 